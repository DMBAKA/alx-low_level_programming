#include "main.h"

#include <stdio.h>
/**
 * _isdigit - checks for a digit (0 through 9)
 * main - check code
 * @c: char
 * Returns 1 if c is uppercase
 * Returns 0 otherwise
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}


